const ROUTER_PATHS = {
  MyClan: '/my-clan',
  Clans: '/clans'
};

const COC_API_PATHS = {
  Clans: '/clans'
};

const COLLECTION_KEYS = {
  MyClan: 'MyClan'
};

const MY_CLAN_TAG = `#${process.env.COC_CLAN_TAG}`;

export {
  ROUTER_PATHS,
  COC_API_PATHS,
  COLLECTION_KEYS,
  MY_CLAN_TAG
};
